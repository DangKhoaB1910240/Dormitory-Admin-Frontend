import { ContractResponseDto } from './contract-response-dto';

describe('ContractResponseDto', () => {
  it('should create an instance', () => {
    expect(new ContractResponseDto()).toBeTruthy();
  });
});
